import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles(() => ({
  textCenter: {
    textAlign: "center",
  },
  spaceBottom: {
    marginBottom: 16,
  },
}))
